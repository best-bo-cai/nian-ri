非功能性需求确认：

| 项目 | 方案 |
|------|------|
| 语言 | 仅中文 |
| 深色模式 | V1.0 支持（暖色调 + 深色适配） |
| 性能要求 | 启动快、动画流畅、无卡顿 |
| 数据安全 | 仅 API Key 简单加密，其余本地明文存储 |
| APP 名称 | 念日 |
| 图标 | 后续再定 |

---

到这里，我们已经完成了所有主要模块的问答。下面我把**整个需求总结**重新整理成一份完整的文档，涵盖所有讨论和确认的内容：

---

## 念日 · 完整需求文档（V1.0）

### 一、核心痛点

| 痛点 | 描述 |
|------|------|
| **生日遗忘** | 亲戚朋友生日记不住，找不到日期，需手动计算距生日还剩多少天 |
| **纪念日零散** | 与女朋友的初次见面、表白、在一起等日子散落笔记中，需手动核对今天是否为特别日子 |
| **节假日计算** | 关心距节假日还有多少天，尤其需要知道剩余工作日数量（剔除调休） |
| **临时事件遗忘** | 家人交代的临时琐事（如"记得洗衣服"）容易忘记，缺少快速记录方式 |
| **手动输入繁琐** | 每次创建提醒都要手动选日期、填标题，期望用自然语言快速记录 |

### 二、目标用户

- 需管理生日、纪念日的普通用户
- 需快速记录临时事件的学生/上班族
- 注重恋爱/家庭关系，希望记录并收到提醒的群体
- **定位**：个人及家人朋友小范围使用，不上大规模应用市场

### 三、技术方案

| 层级 | 选型 |
|------|------|
| 语言 | Kotlin |
| UI 框架 | Jetpack Compose |
| 设计语言 | Material Design 3 |
| 最低版本 | Android 10 (API 29) |
| 数据库 | Room（本地存储） |
| 数据同步 | JSON 文件导入导出 |
| 账号体系 | 无，纯本地应用 |
| AI 接入 | 通用模式，用户自填 Base URL + API Key + Model |
| AI 语音识别（V1.0） | Android 原生 `SpeechRecognizer` |
| 邮件发送 | 用户配置 SMTP，APP 主动发送 |
| 节假日数据 | 第三方 API 动态获取 |
| 通知提醒 | Android 原生 `AlarmManager` + `NotificationManager` |
| 性能要求 | 启动快、动画流畅、无卡顿 |
| 语言 | 仅中文 |

### 四、数据模型

**日子/事件类（核心实体，统一实体，通过类型字段区分）**

| 属性 | 说明 | 是否必填 |
|------|------|----------|
| ID | 唯一标识 | 自动生成 |
| 类型 | 生日 / 纪念日 / 普通事件 / 节假日 | 必填 |
| 事件名称 | 标题 | 必填 |
| 日历类型 | 农历 / 阳历 | 必填 |
| 日期 | 单日（日子型）或起止日期（事件型，区间性） | 必填 |
| 展示方式 | 倒计时 / 正计时 | 必填 |
| 循环规则 | 不循环 / 每年 / 每月 / 每周 | 必填 |
| 是否开启提醒 | 布尔值 | 必填 |
| 提醒方式 | 本地通知 / 邮件（可多选） | 选填（开启提醒后必选） |
| 提醒时间点 | 提前1天 / 提前3天 / 当天9点 / 当天10点（可多选） | 选填（开启提醒后必选） |
| 事件图标 | 展示用图标字符 | V1.0 不支持，后续版本 |
| 邮箱 | 可选提醒邮箱 | 选填 |
| 备注信息 | 自由文本 | 选填 |
| 事件完成状态 | 布尔值（仅事件型使用） | 默认未完成 |

**AI 配置类**

| 属性 | 说明 |
|------|------|
| Base URL | 大模型接口地址 |
| API Key | 简单加密存储 |
| Model | 模型名称 |

**SMTP 配置类**

| 属性 | 说明 |
|------|------|
| 邮箱地址 | 用户邮箱 |
| SMTP 服务器 | 如 smtp.qq.com |
| 端口 | 如 465 / 587 |
| 密码 | 简单加密存储 |

### 五、核心功能

#### 5.1 底部导航栏

四个 Tab：**日子 / 事件 / 对话 / 我的**

#### 5.2 日子页

- **顶部**：日历入口按钮（右侧）+ 搜索按钮（右侧）
- **上半部分**：即将到来的日子，双列卡片展示，显示倒计时天数 + 事件名称
- **下半部分**：日子横条列表，按即将到来日期排序
- **排序规则**：按距离下一个日期的远近，最近的排最前
- **默认内置**：4 个默认日子（下一个法定节假日、春节、父亲节、母亲节），用户可删除，删除后不可恢复
- **创建入口**：右下角"+"按钮（手动创建）
- **编辑**：点击卡片进入编辑页
- **删除**：卡片左滑出现删除按钮 → 点击删除 → 弹窗二次确认 → 确认后删除
- **搜索**：右上角按钮，跳转独立搜索页面

#### 5.3 事件页

- **顶部**：搜索按钮（右上角）
- **展示**：事件横条列表，每个事件卡片显示事件名称 + 起止日期 + 状态（未开始/进行中/已结束）
  - 未开始：显示"距开始还有X天"
  - 进行中：显示"进行中，第X天"
  - 已结束：显示"已结束X天"
- **完成标记**：事件卡片前方有勾选框，勾选后卡片文字加删除线表示已完成
- **排序规则**：进行中的排最前 → 未开始的按距离开始日期排序 → 已结束的排最后
- **默认状态**：空状态，无默认内置
- **创建入口**：右下角"+"按钮（手动创建）
- **编辑**：点击卡片进入编辑页
- **删除**：卡片左滑出现删除按钮 → 点击删除 → 弹窗二次确认 → 确认后删除
- **搜索**：右上角按钮，跳转独立搜索页面

#### 5.4 日历面板

- **入口**：日子页顶部日历入口按钮，点击切换页面展开
- **视图**：月视图日历
- **标记**：日期上有日子/事件标记点，法定节假日和自定义节假日用颜色区分标记
- **交互**：支持左右滑动切换月份
- **点击某天**：下方展开该天所有日子和事件详情
- **快速创建**：V1.0 不支持，后续版本考虑

#### 5.5 搜索页面

- **入口**：日子页右上角 + 事件页右上角
- **搜索方式**：按名称模糊搜索
- **搜索范围**：日子 + 事件统一搜索
- **结果交互**：点击搜索结果跳转到对应日子/事件的编辑页

#### 5.6 创建/编辑面板

**手动创建入口**：日子页右下角"+"、事件页右下角"+"、搜索结果点击进入编辑

**字段**：与数据模型一致，回填数据供编辑

**AI 创建入口**：对话页自然语言输入

#### 5.7 对话页

- **页面布局**：
  - 左上角：清空对话按钮
  - 顶部：页面标题"对话"
  - 中间：消息流区域（用户消息 + AI 回复）
  - 底部：输入框 + 麦克风按钮（长按录音）+ 发送按钮 + AI 配置按钮
- **未配置 AI**：用户发消息时先提示去配置 AI
- **AI 解析流程**：
  - 用户输入文字（键盘或语音长按麦克风）→ AI 提取所有必填字段
  - 解析成功：对话流中返回预览卡片，显示提取的字段，卡片上有"确认创建"和"取消"按钮
  - 解析多个事件：多张预览卡片，每张独立确认或取消
  - 提取失败：提醒用户"是否有什么日子或事件需要帮你记录"
- **消息持久化**：V1.0 不保存，每次打开为空，后续版本考虑历史预览
- **清空对话**：左上角清空按钮

#### 5.8 提醒系统

| 项目 | 方案 |
|------|------|
| 本地推送通知 | 支持 |
| 邮件通知 | 支持（用户配置 SMTP，发送给自己） |
| 选择方式 | 创建日子/事件时自由勾选开启哪种提醒方式 |
| 提醒时间点 | 支持多选：提前1天、提前3天、当天9点、当天10点 |
| 循环提醒 | 每次循环按相同规则触发 |
| 邮件内容 | 到提醒时间 → AI 根据事件信息+备注自动生成祝福语/提醒内容 → 通过 SMTP 发送邮件给自己 |

#### 5.9 AI 模块

| 场景 | 说明 |
|------|------|
| 语音识别（V1.0） | Android 原生 `SpeechRecognizer`，后续升级支持 Whisper API |
| 自然语言解析 | 从文本中提取事件名称、日期、类型、循环规则等 |
| 邮件内容生成 | 到提醒时间自动生成祝福语/提醒内容 |
| 接入方式 | 通用模式，用户自填 Base URL + API Key + Model |
| Key 存储 | 本地简单加密 |

#### 5.10 节假日模块

| 项目 | 方案 |
|------|------|
| 数据来源 | 第三方 API 动态获取中国法定节假日及调休 |
| 展示内容 | 下一个法定节假日名称 + 自然日倒计时 + 工作日倒计时 |
| 自定义节假日 | 支持用户手动添加（公司假期、寒暑假等），日历面板中用颜色区分标记 |

#### 5.11 "我的"页面

- **顶部统计面板**：展示四个通用标签的统计数量
  - 日子总数
  - 事件总数
  - 纪念日数量
  - 生日数量
- **功能列表**：
  - SMTP 邮件配置（邮箱地址、SMTP 服务器、端口、密码）
  - AI 大模型配置（Base URL、API Key、Model 名称）
  - 数据备份与恢复（JSON 导入导出）
  - 提醒设置（全局默认提醒方式、提醒时间点）
  - 主题外观（浅色/深色模式，V1.0 支持深色模式）
  - 关于页面（版本号、更新日志、开发者信息）

### 六、UI 设计规范

| 项目 | 方案 |
|------|------|
| 整体风格 | 暖色调、温馨感 |
| 主色调 | 乌梅酱紫 |
| 设计语言 | Material Design 3 |
| 字体 | 系统默认（后续版本支持自定义） |
| 深色模式 | V1.0 支持，暖色调深色适配 |

### 七、版本规划

#### V1.0 核心功能
- 日子管理（生日、纪念日、节假日、普通事件）
- 事件管理（区间型事件，起止日期，完成状态标记）
- 日历面板（月视图，节假日颜色标记）
- 搜索功能（名称模糊搜索，日子+事件统一搜索）
- 对话页（AI 自然语言解析创建日子/事件，语音输入）
- 提醒系统（本地通知 + 邮件通知，多时间点提醒）
- 节假日看板（第三方 API + 自定义节假日）
- AI 通用接入（Base URL + Key + Model）
- SMTP 邮件配置
- JSON 导入导出备份
- 深色模式
- 默认内置 4 个日子（下一个节假日、春节、父亲节、母亲节）

#### V1.5 强化功能
- AI 祝福语生成、礼物链接快捷跳转
- 通讯录/日历导入、日期计算器
- 纪念日里程碑卡片、时间线分享
- 事件图标支持
- 自定义字体支持

#### V2.0 进阶功能
- 打卡签到与"天塌了"反向签到
- 语音/截图自动创建事件
- 关系本（聚合某人的所有日子与备注）
- Whisper API 语音识别
- 对话历史持久化
- 批量删除
- 日历面板快速创建

#### V2.5 增值功能
- 排班管理、大小周支持
- 端到端加密隐私模式

### 八、当前交付物

- ✅ 完整 PRD 文字需求（痛点、功能、分阶段计划）
- ✅ 交互原型 HTML（可点击切换页面、弹出编辑面板、模拟语音输入）
- ✅ 交互说明文档（每个页面元素的交互行为表）
- ✅ UML 类图（核心数据模型）
- ✅ 技术方案架构图与流程说明
- ✅ 完整需求总结文档

---

以上就是经过所有问答确认后的完整需求文档。如果后续还需要补充某个模块或调整任何细节，可以随时继续。